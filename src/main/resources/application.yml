spring:
  messages:
    basename: i18n.login
  datasource:
    username: root
    password: teapot0033
    url: jdbc:mysql://120.48.2.40:3306/kuaiwu?serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode: always
    type: com.alibaba.druid.pool.DruidDataSource
    #   数据源其他配置
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    #   配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    filters: stat,wall,slf4j
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500
    #创建表语句
    schema:
#      - classpath:sql/attendee.sql
#      - classpath:sql/organizer.sql
#      - classpath:sql/conference.sql
      - classpath:sql/participate.sql
#mybatis全局配置文件的位置
mybatis:
  config-location: classpath:mybatis/mybatisconfig.xml
  mapper-locations: classpath:mybatis/dao/*.xml
